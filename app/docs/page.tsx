export default function DocsPage(){return <main className="page"><div className="container doc-grid"><aside className="doc-nav"><a href="#overview">Overview</a><a href="#workflow">AI workflow</a><a href="#security">Security</a><a href="#limits">Limits</a><a href="#quality">Quality signals</a></aside><article className="prose"><h1>Documentation</h1><p>CodeClarity AI is a portfolio-grade developer workspace for analyzing source code with structured AI assistance. It keeps source code, findings and follow-up actions in a focused IDE-style interface.</p><h2 id="overview">Overview</h2><p>The browser hosts the editor and presentation layer. Analysis requests go through Next.js Route Handlers so the Groq credential stays server-side. Analysis, refactor, tests and documentation use separate server-side operations.</p><h2 id="workflow">AI workflow</h2><ol><li>The selected language and source are validated with Zod.</li><li>The server treats source code as untrusted data and explicitly instructs the model not to follow source-embedded instructions.</li><li>Structured analysis responses are validated against Zod schemas before they reach the UI.</li><li>Interactive actions reuse the current code and selected language as context.</li></ol><h2 id="security">Security</h2><p>Uploaded files are read as text. The application does not execute submitted code. File names are not trusted, request bodies have size limits, and the Groq API key is only read from server environment variables.</p><h2 id="limits">Limits</h2><p>Source input is capped at 50,000 characters to keep requests predictable. Very large repositories are outside the intended scope of this single-file workspace.</p><h2 id="quality">Quality signals</h2><p>The quality score is an AI-generated heuristic, not an objective static-analysis metric. Complexity confidence is explicitly represented, and the UI avoids presenting uncertain model conclusions as verified facts.</p></article></div></main>}
